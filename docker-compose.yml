services:

  dashboard:
    build: .
    command: gunicorn --bind 0.0.0.0:5050 --workers 4 --timeout 120 "app:app"
    ports:
      - "5050:5050"
    env_file: .env
    volumes:
      - .:/app           # project dir mounted directly — files stay on your host
    restart: unless-stopped
